currentPerson;
previousSubmissions = [];

(totalDifference)Math.abs(/*this is where the 2 values for potential friends go
    it will have to be a for loop that runs through all the old submissions and compares them them to the new one.*/);
